language: haskell

install:
    - cabal install c2hs
    - git clone https://github.com/sol/libmpd-haskell && cd libmpd-haskell && cabal install && cd ..

script:
    - cabal sdist && cd dist && tar xvzf vimus-*.tar.gz && cd vimus-* && cabal install
